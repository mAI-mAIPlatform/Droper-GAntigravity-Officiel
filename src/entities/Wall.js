export class Wall {
    constructor(x, y, width, height) {
        this.x = x;
        this.y = y;
        this.width = width;
        this.height = height;
        this.color = 'rgba(255, 255, 255, 0.05)';
        this.borderColor = 'rgba(0, 229, 255, 0.3)';
    }
}
